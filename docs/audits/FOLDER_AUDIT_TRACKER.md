# Folder Audit Tracker

Last refreshed: 2026-03-24 against the current working tree, including the combat/network unification and docs cleanup pass.

## Status

| Folder | Status | Audit Date | Owner / Thread | Notes |
| --- | --- | --- | --- | --- |
| `js/net` | intentionally complete | 2026-03-24 | Codex / current thread | Shared protocol helpers own world-meta and throw/class payload shaping, `js/net/network.js` is the composition root, `js/net/commands.js`, `js/net/state-view.js`, `js/net/connection-timing.js`, `js/net/effects.js`, and `js/net/remote-entities.js` now sit behind one explicit `GameNet` surface, and the temporary bridge/wrapper layers are removed. Remaining work in this folder is intentional future evolution, not cleanup debt. |
| `js/app` | intentionally complete | 2026-03-20 | Codex / current thread | Menu lobby state/actions/polling now live behind `js/app/lobby-session.js`, menu rendering lives behind `js/app/lobby-renderer.js`, menu behavior lives behind `js/app/lobby-actions.js`, gameplay runtime behavior lives behind `js/app/runtime-match-actions.js`, gameplay runtime hosting lives behind `js/app/runtime-match-host.js`, and `runtime-coordinator` is reduced to dependency resolution plus top-level orchestration. Remaining work in this folder is intentional future evolution, not cleanup debt. |
| `js/runtime/main.js` | removed | 2026-03-20 | Codex / current thread | The temporary `GameMain` compatibility facade was deleted once the shipped gameplay bundle became the only live owner of the global entry surface. |
| `shared` | intentionally complete | 2026-03-24 | Codex / current thread | Gun gameplay now flows from `shared/gameplay-tuning.js` and the shared helpers hung off `GameShared`; the temporary client combat compatibility layer is removed, and the weapon audit doc now matches the current room/client path. Remaining issue: weapon feel tuning like tracer timing, reload flash, audio, and recoil is still split across local presentation files. |
| `cloudflare/lib` | deleted | 2026-03-10 | Codex / current thread | Deleted `shared-protocol`, `shared-tuning`, and `shared-terrain` wrappers. `cloudflare/server` and `cloudflare/worker.js` now import shared modules directly. |
| `cloudflare/server/room` | largely remediated | 2026-03-10 | Codex / current thread | Entity lifecycle/reset ownership is now centralized in `cloudflare/server/room/EntityLifecycle.js`, Durable Object fetch/socket admission plus room state/private-config endpoints now live in `cloudflare/server/room/RoomTransport.js`, websocket message/close routing now lives in `cloudflare/server/room/RoomSocket.js`, welcome/snapshot/match-state payload shaping now lives in `cloudflare/server/room/RoomState.js`, public/private match progression rules now live in `cloudflare/server/room/RoomMatch.js`, spawn/player admission/respawn/tick runtime helpers now live in `cloudflare/server/room/RoomRuntime.js`, and weapon/throw/targeting combat helpers now live in `cloudflare/server/room/RoomCombatRuntime.js`. Remaining issue: `GlobalArenaRoom.js` is still large, but what remains is mostly coordinator glue plus genuinely central simulation methods rather than multiple mixed ownership seams. |

## Follow-up Queue

| Date | Folder | Priority | Item | Reason | Owner |
| --- | --- | --- | --- | --- | --- |
| 2026-03-10 | `cloudflare/server/room` | low | Revisit `GlobalArenaRoom.js` only when a concrete bug or new feature proves the remaining coordinator glue is unstable. | The previous mixed seams are now extracted. Further splitting now risks creating thin wrappers rather than buying meaningful simplification. | Codex |
| 2026-03-15 | `js/runtime/main.js` | resolved on 2026-03-20 | Remove the compatibility facade once no callers depend on `GameMain` as a legacy surface. | The compatibility facade was deleted after the live gameplay bundle and current tests stopped needing the separate shim entry file. | Codex |
| 2026-03-10 | `shared`, `js/core/local-match`, `cloudflare/server/room` | medium | Audit match-state and public scoring duplication across server and local-match runtime. | The room owns FFA/TDM progression and snapshot payload shape, while `js/core/local-match.js` appears to maintain a parallel match-state model for local play. That is a likely next protocol/rules drift seam once room remediation starts. | Codex |
| 2026-03-10 | `shared`, `js/combat`, `js/presentation`, `js/actors` | low | Decide whether weapon feel tuning should remain local presentation data or move into a shared schema. | Gun correctness is now shared-owned, but tracer timing, reload flash, weapon audio, and recoil still live in separate runtime files. That is fine for visuals, yet it is the next place “balance” changes can sprawl if designers want cross-system consistency. | Codex |

## Session Log

| Date | Folder | Owner / Thread | Summary | Outcome |
| --- | --- | --- | --- | --- |
| 2026-03-10 | `js/net` | Codex / current thread | Audited the net entry module, `js/net/*`, `shared/protocol.js`, key server boundary files, and the primary callers in `js/main.js`. Identified a minimal plan centered on shrinking `GameNet`, deleting mirror wrappers, and centralizing payload/state shaping. | planned |
| 2026-03-10 | `cloudflare/lib` | Codex / current thread | Audited all three `cloudflare/lib` wrappers, their `cloudflare/server/room` callers, and the underlying shared modules. Confirmed the folder is not a real seam: room code mixes wrapper imports with direct shared imports, the tuning wrapper deep-clones shared config in every consumer, and default world fallbacks are duplicated outside shared ownership. | planned |
| 2026-03-10 | `js/net`, `cloudflare/lib`, `cloudflare/server/room`, `shared` | Codex / current thread | Replaced client/server payload reshaping with shared protocol helpers, corrected stale default fallbacks, removed `GameNet.queueClassChange()`, switched worker/room services to direct shared imports, and deleted the `cloudflare/lib` wrapper layer. Verified with targeted node tests plus `npm run build`. | implemented |
| 2026-03-10 | `js/net`, `js/app` | Codex / current thread | Split menu auth DOM and modal wiring out of `js/net/auth.js` into `js/app/auth-ui.js`, removed dead `GameNet` auth facade methods and the dead queue branch, and re-verified with targeted node tests, `npm run build`, and `e2e/menu-shell.spec.js`. | implemented |
| 2026-03-10 | `js/net` | Codex / current thread | Extracted inbound message application from the net entry module into `js/net/message-router.js` and moved the read-only selector/queue surface into `js/net/state-view.js`. The net entry shrank from 1102 lines to 764 lines and `js/net/auth.js` dropped from 785 lines to 617 lines. Verified with targeted node tests and `npm run build`. | implemented |
| 2026-03-10 | `js/net` | Codex / current thread | Extracted connection lifecycle and frame-update logic from the net entry module into `js/net/runtime-core.js`. The net entry shrank further from 764 lines to 660 lines. Verified with targeted node tests and `npm run build`. | implemented |
| 2026-03-10 | `js/app` | Codex / current thread | Planning-only audit of the menu logic/clickable boundary, including the party lock button, social tabs, private-room controls, modal triggers, and adjacent bootstrap files. Read `js/app/menu-shell.js`, `js/app/lobby-controller.js`, all menu subviews, `js/main.js`, `index.html`, `css/main.css`, `cloudflare/server/party.js`, and the existing menu tests. | planned |
| 2026-03-10 | `shared`, `js/combat/hitscan.js`, client combat compatibility layer, `cloudflare/server/room` | Codex / current thread | Planning-only gun-balance audit. Read shared weapon tuning, hitscan authority, client hitscan/combat tuning, room fire/ammo enforcement, entity serialization, tests, and the existing weapon audit doc. Confirmed that server reload/ammo enforcement already exists; the current problem is duplicated client fallback ownership and stale documentation, not missing authoritative gun rules. | planned |
| 2026-03-10 | `cloudflare/server/room` | Codex / current thread | Next recommended thread target after the `js/net` cleanup. `GlobalArenaRoom.js` is now the clearest oversized boundary left in the multiplayer path and should be audited/refactored next. | handoff |
| 2026-03-10 | `cloudflare/server/room`, `cloudflare/server/private-room-lobby`, `cloudflare/server/matchmaking`, `shared`, `js/core/local-match` | Codex / current thread | Planning-only audit of the room boundary. Read the Durable Object entrypoint, all room services, shared gameplay/protocol modules, and the nearest callers in private-room lobby and matchmaking. Confirmed that the highest-value next step is not new gameplay logic; it is shrinking `GlobalArenaRoom.js` by deleting dead class-era leftovers, centralizing entity lifecycle/reset helpers, and isolating transport/config handling from room simulation. | planned |
| 2026-03-10 | `cloudflare/server/room` | Codex / current thread | Added `EntityLifecycle.js` to centralize player/bot creation plus respawn resets, rewired `GlobalArenaRoom.js` and `BotAI.js` to use it, and deleted the dead class-era room leftovers. Verified with `node --experimental-default-type=module --test tests/room-entity-lifecycle.test.js`, `node --experimental-default-type=module --test tests/lobby-smoke.test.js`, and `npm run build`. | implemented |
| 2026-03-10 | `cloudflare/server/room` | Codex / current thread | Added `RoomTransport.js` to own Durable Object fetch/state/private-config/socket-admission rules, rewired `GlobalArenaRoom.fetch()` and socket replacement lookup to use it, and added targeted transport tests. Verified with `node --experimental-default-type=module --test tests/room-transport.test.js`, `node --experimental-default-type=module --test tests/lobby-smoke.test.js`, and `npm run build`. | implemented |
| 2026-03-10 | `cloudflare/server/room` | Codex / current thread | Added `RoomSocket.js` to own websocket message and close routing, rewired `GlobalArenaRoom.webSocketMessage()` and `webSocketClose()` to delegate into it, and added targeted socket tests. Verified with `node --experimental-default-type=module --test tests/room-socket.test.js`, `node --experimental-default-type=module --test tests/lobby-smoke.test.js`, and `npm run build`. | implemented |
| 2026-03-10 | `cloudflare/server/room` | Codex / current thread | Added `RoomState.js` to own welcome payloads, snapshot envelopes, private-room phase derivation, and serialized match-state shaping, then rewired `GlobalArenaRoom.js` to use it. Verified with `node --experimental-default-type=module --test tests/room-state.test.js`, `node --experimental-default-type=module --test tests/lobby-smoke.test.js`, and `npm run build`. | implemented |
| 2026-03-10 | `cloudflare/server/room` | Codex / current thread | Added `RoomMatch.js` to own private/public match progression rules, team assignment baselines, leader tracking, elimination scoring, and match reset/finish flow, then rewired `GlobalArenaRoom.js` to delegate into it. Verified with `node --experimental-default-type=module --test tests/room-match.test.js`, `node --experimental-default-type=module --test tests/lobby-smoke.test.js`, and `npm run build`. | implemented |
| 2026-03-10 | `cloudflare/server/room` | Codex / current thread | Added `RoomCombatRuntime.js` to own weapon ammo/reload flow, throw validation/spawn flow, and combat targeting/query helpers, then rewired `GlobalArenaRoom.js` to delegate into it. Verified with `node --experimental-default-type=module --test tests/room-combat-runtime.test.js`, `node --experimental-default-type=module --test tests/room-runtime.test.js`, `node --experimental-default-type=module --test tests/lobby-smoke.test.js`, and `npm run build`. | implemented |
| 2026-03-10 | `js/app` | Codex / current thread | Centralized party/private-room button ownership back into `lobby-controller.js`, stripped the view modules down to rendering plus summary/team updates, kept canonical reconcile reads for party lock and private-room create/join, and tightened the menu harness flush so stale-payload tests observe the reconciled end state. Verified with `node --experimental-default-type=module --test tests/menu-click-paths.test.js` and `npx playwright test e2e/social.spec.js`. | implemented |
| 2026-03-10 | `js/app` | Codex / current thread | Split menu/social/private-room click wiring into temporary helper boundaries, routed private-room host actions through one explicit busy/reconcile helper, and rewired the controller/view boundary without changing render ownership. Those helper files were later deleted once the controller absorbed the live ownership again. Verified with `node --experimental-default-type=module --test tests/menu-click-paths.test.js`, `npx playwright test e2e/social.spec.js`, and `npm run build`. | implemented |
| 2026-03-10 | `js/main.js`, `js/app` | Codex / current thread | Deleted the dead `js/main.js` menu bootstrap path, removed the duplicated menu-boot release and inline `setupMenu*Loadout()` binders, and left lazy gameplay loading to register only `GameMain` on import. Verified with `npx playwright test e2e/menu-shell.spec.js` and `npm run build`. | implemented |
| 2026-03-10 | `js/app` | Codex / current thread | Fixed the silent menu-sync blank-state bug by preserving last-known party/private-room state on background refresh failures, surfacing explicit offline/retrying UI, removing the dead private-room sync wrappers, and pulling view-to-controller state mutation back into `lobby-controller.js`. Verified with `node --experimental-default-type=module --test tests/menu-click-paths.test.js`, `npx playwright test e2e/social.spec.js`, and `npm run build`. | implemented |
| 2026-03-10 | `js/app` | Codex / current thread | Added `js/app/lobby-session.js` to own lobby state/actions/polling, rewrote `lobby-controller.js` around that boundary, deleted `js/app/lobby-sync-runtime.js`, moved friend payload normalization out of the view, and added explicit private-room clear-state behavior. Verified with `node --experimental-default-type=module --test tests/menu-click-paths.test.js` and `npm run build`. | implemented |
| 2026-03-10 | `shared`, `js/combat/hitscan.js`, client combat compatibility layer, `docs` | Codex / current thread | Removed duplicate client-side weapon ownership by adding shared falloff helpers, rewiring `js/combat/hitscan.js` weapon catalog/falloff reads to shared tuning, narrowing the temporary client combat compatibility layer, and replacing the stale weapon audit doc. Verified with targeted shared/hitscan tests and `npm run build`. | implemented |
| 2026-03-13 | `js`, `tests`, `docs`, `.cf-deploy` | Codex / current thread | Removed the remaining root-level runtime mirrors in favor of the namespaced `js/app`, `js/net`, `js/runtime`, `js/presentation`, `js/combat`, and `js/actors` trees, collapsed duplicate top-level tests onto the namespaced suites after preserving the one root-only room rewind case, rewrote live docs to canonical module paths, and completed the tracked `.cf-deploy` bundle removal. | implemented |
| 2026-03-20 | `js/app`, `js/world`, `cloudflare/server/room/runtime`, `tests`, `public`, `scripts`, `docs` | Codex / current thread | Removed the alternate room-runtime/service stack and its dedicated tests, deleted dormant split-out helpers, and updated live docs to point only at the canonical server path. Verified with targeted ownership checks, networking suites, and focused room/runtime tests. | implemented |
| 2026-03-20 | `js/net`, `js/runtime`, `cloudflare/server/room`, `js/world`, `scripts`, `tests`, `docs` | Codex / current thread | Removed the remaining disconnected compatibility/test-only module entries before release: deleted the unused browser compatibility `js/net/runtime.js`, `js/net/remote-presenter.mjs`, and `js/runtime/main.js` surfaces; deleted the unused LMS and experimental room/world runtime stack; retired the unused biome map generation tooling and its tests; aligned browser e2e sandbox launch coverage with the current live UI; and updated rewrite/network/docs references that still described the deleted modules as active. Verified with build, app/server tests, targeted compatibility survivor tests, and refreshed dead-code scans. | implemented |
| 2026-03-20 | `js/app`, `js/net`, `js/actors/player`, `js/combat/throwables-projectile-runtime`, `tests`, `scripts`, `docs` | Codex / current thread | Added `js/app/runtime-assembly.js` as the shared runtime dependency contract, seeded explicit app/net deps before bundle boot, moved the net harness tests onto the shared script-order source of truth, made Playwright/dev startup port-configurable with optional server reuse, and routed the player plus projectile runtime through narrow dependency helpers instead of hard-coded cross-runtime reads. Verified with targeted app/net/player/throwable tests plus `npm run build`. | implemented |
| 2026-03-20 | `js/app`, `tests`, `docs` | Codex / current thread | Added `js/app/lobby-renderer.js` as the menu rendering owner, rewired `lobby-controller.js` to delegate DOM/render work through that owner, updated the menu harness to load the explicit renderer dependency, and kept the controller focused on state, actions, and event wiring. Verified with targeted menu tests, `npm run build`, full smoke tests, and full e2e. | implemented |
| 2026-03-20 | `js/app`, `tests`, `docs` | Codex / current thread | Added `js/app/lobby-actions.js` as the menu behavior owner, rewired `lobby-controller.js` so button/session handlers delegate to explicit behavior methods, updated runtime assembly to inject the new action factory, and updated the menu harness to load the explicit behavior dependency. Verified with targeted menu tests, `npm run build`, full smoke tests, full e2e, and a manual `Play FFA` handoff check on isolated local ports. | implemented |
| 2026-03-20 | `js/app`, `js/net`, `tests`, `docs` | Codex / current thread | Added `js/app/runtime-match-actions.js` and `js/app/runtime-match-host.js` so the gameplay coordinator now composes runtime behavior instead of owning it directly; added `js/net/effects.js` plus an interim public `GameNet` wrapper so the net entry could compose helper/effect logic instead of owning it inline; updated runtime assembly and the net/browser harness load order; and verified with targeted coordinator/net tests, full smoke, full e2e, and a manual isolated-port `Play FFA` handoff check. | implemented |
| 2026-03-24 | `shared`, `js/net`, `js/app`, `tests`, `docs` | Codex / current thread | Removed the temporary client combat compatibility layer, fully unified `GameNet` around `view`, `commands`, `timing`, `effects`, and `remoteEntities`, deleted the temporary net bridge/wrapper files, updated callers and harnesses to the single public shape, and cleaned the ownership/audit docs so they no longer describe deleted layers as live. Verified with focused gameplay/network tests and `npm run build`. | implemented |

## Recommended Next Targets

1. `shared`
2. `cloudflare/server/room`
