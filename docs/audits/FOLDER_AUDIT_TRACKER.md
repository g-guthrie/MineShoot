# Folder Audit Tracker

Last refreshed: 2026-03-13 against the current working tree, including the mirror cleanup pass.

## Status

| Folder | Status | Audit Date | Owner / Thread | Notes |
| --- | --- | --- | --- | --- |
| `js/net` | largely remediated | 2026-03-10 | Codex / current thread | Shared protocol helpers now own world-meta and throw/class payload shaping, auth DOM/menu wiring is out of `js/net/auth.js`, and `js/network.js` has been split across `message-router`, `state-view`, and `runtime-core`. Current `js/network.js` is down to 660 lines from 1102. Remaining issue: it still owns some facade-level gameplay send helpers and fallback runtime lookups. |
| `js/app` | partially remediated | 2026-03-10 | Codex / current thread | Menu lobby state/actions/polling now live behind `js/app/lobby-session.js`, `js/app/lobby-sync-runtime.js` is deleted, `lobby-controller.js` is reduced to UI/render/launch coordination, friend payload normalization moved out of the view, and private-room focus/auth refresh now stays current without double-fetching. Remaining issues: `lobby-controller.js` still owns a large DOM surface plus launch/runtime orchestration, and the folder still depends on global side-effect module ordering. |
| `js/main.js` | largely remediated | 2026-03-10 | Codex / current thread | Deleted the dead menu bootstrap path, menu-boot release duplicate, and inline menu loadout binders from the lazy gameplay bundle. `js/main.js` now only registers `GameMain` and gameplay-time runtime orchestration when the gameplay bundle is imported. Remaining issue: it still carries a few defensive menu-loadout fallback branches that could be deleted once `GameMenuLoadout` is treated as guaranteed in the gameplay bundle. |
| `shared` | largely remediated | 2026-03-10 | Codex / current thread | Gun gameplay now flows from `shared/gameplay-tuning.js`: `js/hitscan.js` derives weapon catalog/order and falloff from shared helpers, `js/combat-tuning.js` is reduced to a compatibility facade, and `docs/weapon-tuning-audit.md` now matches the current room/client path. Remaining issue: weapon feel tuning like tracer timing, reload flash, audio, and recoil is still split across local presentation files. |
| `cloudflare/lib` | deleted | 2026-03-10 | Codex / current thread | Deleted `shared-protocol`, `shared-tuning`, and `shared-terrain` wrappers. `cloudflare/server` and `cloudflare/worker.js` now import shared modules directly. |
| `cloudflare/server/room` | largely remediated | 2026-03-10 | Codex / current thread | Entity lifecycle/reset ownership is now centralized in `cloudflare/server/room/EntityLifecycle.js`, Durable Object fetch/socket admission plus room state/private-config endpoints now live in `cloudflare/server/room/RoomTransport.js`, websocket message/close routing now lives in `cloudflare/server/room/RoomSocket.js`, welcome/snapshot/match-state payload shaping now lives in `cloudflare/server/room/RoomState.js`, public/private match progression rules now live in `cloudflare/server/room/RoomMatch.js`, LMS beacon/public-state flow now lives in `cloudflare/server/room/RoomLms.js`, spawn/player admission/respawn/tick runtime helpers now live in `cloudflare/server/room/RoomRuntime.js`, and weapon/throw/targeting combat helpers now live in `cloudflare/server/room/RoomCombatRuntime.js`. Remaining issue: `GlobalArenaRoom.js` is still large, but what remains is mostly coordinator glue plus genuinely central simulation methods rather than multiple mixed ownership seams. |

## Follow-up Queue

| Date | Folder | Priority | Item | Reason | Owner |
| --- | --- | --- | --- | --- | --- |
| 2026-03-10 | `js/net` | medium | Trim remaining facade-only send helpers and fallback runtime lookups in `js/network.js`. | Transport/lifecycle and prediction/update logic now live in `js/net/runtime-core.js`, but `js/network.js` still holds several outgoing message helpers and direct `globalThis.__MAYHEM_RUNTIME` lookups that could be narrowed further. | Codex |
| 2026-03-10 | `js/net` | medium | Replace remaining client-side fallback gameplay defaults with shared-derived values or explicit failure paths. | `js/net/remote-entities.js` now uses shared class presets, but `js/network.js` still carries local fallback room/world logic and broad runtime-global reads. | Codex |
| 2026-03-10 | `cloudflare/server/room` | low | Revisit `GlobalArenaRoom.js` only when a concrete bug or new feature proves the remaining coordinator glue is unstable. | The previous mixed seams are now extracted. Further splitting now risks creating thin wrappers rather than buying meaningful simplification. | Codex |
| 2026-03-10 | `js/app` | medium | Split remaining launch/runtime orchestration out of `lobby-controller.js` after the new `lobby-session.js` boundary. | Polling, reconcile logic, and lobby state now sit behind `GameLobbySession`, but the controller still owns a wide DOM lookup surface, startup/launch flow, and menu runtime coordination through globals. | Codex |
| 2026-03-10 | `js/main.js` | low | Delete the remaining defensive menu-loadout fallback branches now that gameplay always imports `js/app/menu-loadout.js` first. | `validateMenuSelections()`, `currentAbilityMenuLoadout()`, and `syncMenuWeaponSlotsToRuntime()` still have non-`GameMenuLoadout` fallback behavior even though `js/app/gameplay-modules.js` imports `js/app/menu-loadout.js` before `js/main.js`. | Codex |
| 2026-03-10 | `shared`, `js/core/local-match`, `cloudflare/server/room` | medium | Audit match-state and LMS/public scoring duplication across server and local-match runtime. | The room owns FFA/TDM/LMS progression and snapshot payload shape, while `js/core/local-match.js` appears to maintain a parallel match-state model for local play. That is a likely next protocol/rules drift seam once room remediation starts. | Codex |
| 2026-03-10 | `shared`, `js/hitscan.js`, `js/audio.js`, `js/player.js` | low | Decide whether weapon feel tuning should remain local presentation data or move into a shared schema. | Gun correctness is now shared-owned, but tracer timing, reload flash, weapon audio, and recoil still live in separate runtime files. That is fine for visuals, yet it is the next place “balance” changes can sprawl if designers want cross-system consistency. | Codex |

## Session Log

| Date | Folder | Owner / Thread | Summary | Outcome |
| --- | --- | --- | --- | --- |
| 2026-03-10 | `js/net` | Codex / current thread | Audited `js/network.js`, `js/net/*`, `shared/protocol.js`, key server boundary files, and the primary callers in `js/main.js`. Identified a minimal plan centered on shrinking `GameNet`, deleting mirror wrappers, and centralizing payload/state shaping. | planned |
| 2026-03-10 | `cloudflare/lib` | Codex / current thread | Audited all three `cloudflare/lib` wrappers, their `cloudflare/server/room` callers, and the underlying shared modules. Confirmed the folder is not a real seam: room code mixes wrapper imports with direct shared imports, the tuning wrapper deep-clones shared config in every consumer, and default ability/world fallbacks are duplicated outside shared ownership. | planned |
| 2026-03-10 | `js/net`, `cloudflare/lib`, `cloudflare/server/room`, `shared` | Codex / current thread | Replaced client/server payload reshaping with shared protocol helpers, corrected stale default ability fallbacks, removed `GameNet.queueClassChange()`, switched worker/room services to direct shared imports, and deleted the `cloudflare/lib` wrapper layer. Verified with targeted node tests plus `npm run build`. | implemented |
| 2026-03-10 | `js/net`, `js/app` | Codex / current thread | Split menu auth DOM and modal wiring out of `js/net/auth.js` into `js/app/auth-ui.js`, removed dead `GameNet` auth facade methods and the dead `CLASS_QUEUED` branch, and re-verified with targeted node tests, `npm run build`, and `e2e/menu-shell.spec.js`. | implemented |
| 2026-03-10 | `js/net` | Codex / current thread | Extracted inbound message application from `js/network.js` into `js/net/message-router.js` and moved the read-only selector/queue surface into `js/net/state-view.js`. `js/network.js` dropped from 1102 lines to 764 lines and `js/net/auth.js` dropped from 785 lines to 617 lines. Verified with targeted node tests and `npm run build`. | implemented |
| 2026-03-10 | `js/net` | Codex / current thread | Extracted connection lifecycle and frame-update logic from `js/network.js` into `js/net/runtime-core.js`. `js/network.js` dropped further from 764 lines to 660 lines. Verified with targeted node tests and `npm run build`. | implemented |
| 2026-03-10 | `js/app` | Codex / current thread | Planning-only audit of the menu logic/clickable boundary, including the party lock button, social tabs, private-room controls, modal triggers, and adjacent bootstrap files. Read `js/app/menu-shell.js`, `js/app/lobby-controller.js`, all menu subviews, `js/main.js`, `index.html`, `css/main.css`, `cloudflare/server/party.js`, and the existing menu tests. | planned |
| 2026-03-10 | `shared`, `js/hitscan.js`, `js/combat-tuning.js`, `cloudflare/server/room` | Codex / current thread | Planning-only gun-balance audit. Read shared weapon tuning, hitscan authority, client hitscan/combat tuning, room fire/ammo enforcement, entity serialization, tests, and the existing weapon audit doc. Confirmed that server reload/ammo enforcement already exists; the current problem is duplicated client fallback ownership and stale documentation, not missing authoritative gun rules. | planned |
| 2026-03-10 | `cloudflare/server/room` | Codex / current thread | Next recommended thread target after the `js/net` cleanup. `GlobalArenaRoom.js` is now the clearest oversized boundary left in the multiplayer path and should be audited/refactored next. | handoff |
| 2026-03-10 | `cloudflare/server/room`, `cloudflare/server/private-room-lobby`, `cloudflare/server/matchmaking`, `shared`, `js/core/local-match` | Codex / current thread | Planning-only audit of the room boundary. Read the Durable Object entrypoint, all room services, shared gameplay/protocol modules, and the nearest callers in private-room lobby and matchmaking. Confirmed that the highest-value next step is not new gameplay logic; it is shrinking `GlobalArenaRoom.js` by deleting dead class-era leftovers, centralizing entity lifecycle/reset helpers, and isolating transport/config handling from room simulation. | planned |
| 2026-03-10 | `cloudflare/server/room` | Codex / current thread | Added `EntityLifecycle.js` to centralize player/bot creation plus LMS/respawn resets, rewired `GlobalArenaRoom.js` and `BotAI.js` to use it, and deleted the dead class-era room leftovers. Verified with `node --experimental-default-type=module --test tests/room-entity-lifecycle.test.js`, `node --experimental-default-type=module --test tests/lobby-smoke.test.js`, and `npm run build`. | implemented |
| 2026-03-10 | `cloudflare/server/room` | Codex / current thread | Added `RoomTransport.js` to own Durable Object fetch/state/private-config/socket-admission rules, rewired `GlobalArenaRoom.fetch()` and socket replacement lookup to use it, and added targeted transport tests. Verified with `node --experimental-default-type=module --test tests/room-transport.test.js`, `node --experimental-default-type=module --test tests/lobby-smoke.test.js`, and `npm run build`. | implemented |
| 2026-03-10 | `cloudflare/server/room` | Codex / current thread | Added `RoomSocket.js` to own websocket message and close routing, rewired `GlobalArenaRoom.webSocketMessage()` and `webSocketClose()` to delegate into it, and added targeted socket tests. Verified with `node --experimental-default-type=module --test tests/room-socket.test.js`, `node --experimental-default-type=module --test tests/lobby-smoke.test.js`, and `npm run build`. | implemented |
| 2026-03-10 | `cloudflare/server/room` | Codex / current thread | Added `RoomState.js` to own welcome payloads, snapshot envelopes, private-room phase derivation, and serialized match-state shaping, then rewired `GlobalArenaRoom.js` to use it. Verified with `node --experimental-default-type=module --test tests/room-state.test.js`, `node --experimental-default-type=module --test tests/lobby-smoke.test.js`, and `npm run build`. | implemented |
| 2026-03-10 | `cloudflare/server/room` | Codex / current thread | Added `RoomMatch.js` to own private/public match progression rules, team assignment baselines, leader tracking, elimination scoring, and match reset/finish flow, then rewired `GlobalArenaRoom.js` to delegate into it. Verified with `node --experimental-default-type=module --test tests/room-match.test.js`, `node --experimental-default-type=module --test tests/lobby-smoke.test.js`, and `npm run build`. | implemented |
| 2026-03-10 | `cloudflare/server/room` | Codex / current thread | Added `RoomLms.js` and `RoomRuntime.js` to split LMS beacon/public-state flow plus spawn/player admission/respawn/runtime ticking out of `GlobalArenaRoom.js`, then added targeted LMS/runtime tests. Verified with `node --experimental-default-type=module --test tests/room-lms.test.js`, `node --experimental-default-type=module --test tests/room-runtime.test.js`, `node --experimental-default-type=module --test tests/lobby-smoke.test.js`, and `npm run build`. | implemented |
| 2026-03-10 | `cloudflare/server/room` | Codex / current thread | Added `RoomCombatRuntime.js` to own weapon ammo/reload flow, throw validation/spawn flow, and combat targeting/query helpers, then rewired `GlobalArenaRoom.js` to delegate into it. Verified with `node --experimental-default-type=module --test tests/room-combat-runtime.test.js`, `node --experimental-default-type=module --test tests/room-runtime.test.js`, `node --experimental-default-type=module --test tests/lobby-smoke.test.js`, and `npm run build`. | implemented |
| 2026-03-10 | `js/app` | Codex / current thread | Centralized party/private-room button ownership back into `lobby-controller.js`, stripped the view modules down to rendering plus summary/team updates, kept canonical reconcile reads for party lock and private-room create/join, and tightened the menu harness flush so stale-payload tests observe the reconciled end state. Verified with `node --experimental-default-type=module --test tests/menu-click-paths.test.js` and `npx playwright test e2e/social.spec.js`. | implemented |
| 2026-03-10 | `js/app` | Codex / current thread | Split menu/social/private-room click wiring into `js/app/lobby-clickables.js`, routed private-room host actions through one explicit busy/reconcile helper, and rewired the controller/view boundary to use the new surface binder without changing render ownership. Verified with `node --experimental-default-type=module --test tests/menu-click-paths.test.js`, `npx playwright test e2e/social.spec.js`, and `npm run build`. | implemented |
| 2026-03-10 | `js/main.js`, `js/app` | Codex / current thread | Deleted the dead `js/main.js` menu bootstrap path, removed the duplicated menu-boot release and inline `setupMenu*Loadout()` binders, and left lazy gameplay loading to register only `GameMain` on import. Verified with `npx playwright test e2e/menu-shell.spec.js` and `npm run build`. | implemented |
| 2026-03-10 | `js/app` | Codex / current thread | Fixed the silent menu-sync blank-state bug by preserving last-known party/private-room state on background refresh failures, surfacing explicit offline/retrying UI, removing the dead private-room sync wrappers, and pulling view-to-controller state mutation back into `lobby-controller.js`. Verified with `node --experimental-default-type=module --test tests/menu-click-paths.test.js`, `npx playwright test e2e/social.spec.js`, and `npm run build`. | implemented |
| 2026-03-10 | `js/app` | Codex / current thread | Added `js/app/lobby-session.js` to own lobby state/actions/polling, rewrote `lobby-controller.js` around that boundary, deleted `js/app/lobby-sync-runtime.js`, moved friend payload normalization out of the view, and added explicit private-room clear-state behavior. Verified with `node --experimental-default-type=module --test tests/menu-click-paths.test.js` and `npm run build`. | implemented |
| 2026-03-10 | `shared`, `js/hitscan.js`, `js/combat-tuning.js`, `docs` | Codex / current thread | Removed duplicate client-side weapon ownership by adding shared falloff helpers, rewiring `js/hitscan.js` weapon catalog/falloff reads to shared tuning, reducing `js/combat-tuning.js` weapon reads to a compatibility facade, and replacing the stale weapon audit doc. Verified with `node --experimental-default-type=module --test tests/gameplay-tuning.test.js`, `node --experimental-default-type=module --test tests/hitscan-authority.test.js`, and `npm run build`. | implemented |
| 2026-03-13 | `js`, `tests`, `docs`, `.cf-deploy` | Codex / current thread | Removed the remaining root-level runtime mirrors in favor of the namespaced `js/app`, `js/net`, `js/runtime`, `js/presentation`, `js/combat`, and `js/actors` trees, collapsed duplicate top-level tests onto the namespaced suites after preserving the one root-only room rewind case, rewrote live docs to canonical module paths, and completed the tracked `.cf-deploy` bundle removal. | implemented |

## Recommended Next Targets

1. `js/app`
2. `cloudflare/server/room`
3. `shared`
4. `js/net`
