# world

Owns environment generation/layout, collision, bounds, traversal, map data, spawn logic, and world art integration.

Status:
- Rewrite landing zone created.
- Current implementation still lives primarily under `js/world` and `shared/world-*`.
