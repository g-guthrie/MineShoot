{
  "name": "mineshoot",
  "version": "2.0.0",
  "type": "module",
  "description": "Online browser multiplayer Minecraft-style shooter",
  "license": "ISC",
  "dependencies": {
    "three": "0.160.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "vite": "^6.4.1",
    "wrangler": "^4.99.0"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 3000",
    "dev:server": "wrangler dev --config wrangler.worker.toml --port 8787",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 3000",
    "test": "playwright test",
    "test:e2e": "playwright test",
    "deploy:prod": "./scripts/deploy-prod.sh",
    "deploy:status": "./scripts/deploy-status.sh"
  }
}
