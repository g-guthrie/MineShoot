# server

Owns matchmaking, room lifecycle, authoritative simulation, bots, persistence, private/public room rules, and deploy/runtime bindings.

Status:
- Rewrite landing zone created.
- Current implementation still lives primarily under `cloudflare/server` and `cloudflare/worker.js`.
