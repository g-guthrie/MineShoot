Simulation split notes:
- combat.js contains extracted reusable combat math primitives.
- projectiles.js contains extracted reusable projectile integration primitives.
These are wired by GlobalArenaRoom and can be expanded as methods are peeled out.
