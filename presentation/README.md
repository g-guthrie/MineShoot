# presentation

Owns actor visuals, weapon visuals, rigs, reticles, hit feedback, remote entity presentation, scene composition, and animation state.

Status:
- Rewrite landing zone created.
- Live frame rendering ownership now starts in `presentation/runtime-loop.js`.
- Current implementation still lives primarily under visual modules in `js/` and the extracted presentation loop owner.
